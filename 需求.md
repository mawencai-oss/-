
# 四六级监考人员管理系统设计方案

## 功能模块设计

### 1. 用户管理

- 用户角色：管理员、院系管理员、监考教师
- 账号注册与审核
- 权限控制与访问管理
- 个人信息维护

### 2. 监考排班管理

- 自动排班功能（根据教师时间安排和监考资质）
- 手动调整监考安排
- 冲突检测与提醒
- 监考历史记录查询

### 3. 考场管理

- 考场信息录入与维护
- 考场容量与分布统计
- 特殊考场标记（如听力考场）
- 考场状态实时监控

### 4. 教师信息管理

- 教师基本信息维护
- 监考资质管理
- 请假与调班申请
- 监考次数统计与平衡

### 5. 数据导入导出

- Excel导入教师信息
- Excel导入考场信息
- 排班结果导出
- 监考通知单生成

### 6. 通知与消息

- 监考安排通知推送
- 变更提醒
- 系统公告发布
- 消息阅读状态追踪

## 数据库设计

### 用户表(sys_user)

```sql
CREATE TABLE sys_user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) NOT NULL COMMENT '用户名',
  password VARCHAR(100) NOT NULL COMMENT '密码',
  real_name VARCHAR(50) COMMENT '真实姓名',
  phone VARCHAR(20) COMMENT '手机号码',
  email VARCHAR(100) COMMENT '邮箱',
  department VARCHAR(50) COMMENT '所属院系',
  role_id INT COMMENT '角色ID',
  status TINYINT DEFAULT 1 COMMENT '状态(0-禁用,1-正常)',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 角色表(sys_role)

```sql
CREATE TABLE sys_role (
  id INT PRIMARY KEY AUTO_INCREMENT,
  role_name VARCHAR(50) NOT NULL COMMENT '角色名称',
  role_code VARCHAR(50) NOT NULL COMMENT '角色编码',
  description VARCHAR(200) COMMENT '角色描述',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 教师信息表(teacher_info)

```sql
CREATE TABLE teacher_info (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT COMMENT '关联用户ID',
  teacher_code VARCHAR(20) COMMENT '教师工号',
  title VARCHAR(50) COMMENT '职称',
  department VARCHAR(100) COMMENT '院系',
  qualifications TEXT COMMENT '监考资质',
  max_invigilations INT DEFAULT 3 COMMENT '最大监考次数',
  invigilated_count INT DEFAULT 0 COMMENT '已监考次数',
  status TINYINT DEFAULT 1 COMMENT '状态(0-不可排班,1-可排班)',
  remarks VARCHAR(500) COMMENT '备注'
);
```

### 考场信息表(exam_room)

```sql
CREATE TABLE exam_room (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  room_code VARCHAR(20) COMMENT '考场编号',
  building VARCHAR(50) COMMENT '教学楼',
  room_number VARCHAR(20) COMMENT '教室号',
  capacity INT COMMENT '考场容量',
  room_type TINYINT COMMENT '考场类型(1-普通,2-听力,3-特殊)',
  status TINYINT DEFAULT 1 COMMENT '状态(0-不可用,1-可用)',
  remarks VARCHAR(500) COMMENT '备注'
);
```

### 考试安排表(exam_schedule)

```sql
CREATE TABLE exam_schedule (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  exam_date DATE COMMENT '考试日期',
  time_slot VARCHAR(50) COMMENT '时间段',
  exam_type VARCHAR(20) COMMENT '考试类型(CET4/CET6)',
  status TINYINT DEFAULT 1 COMMENT '状态(0-未开始,1-进行中,2-已结束)'
);
```

### 监考安排表(invigilate_arrangement)

```sql
CREATE TABLE invigilate_arrangement (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  schedule_id BIGINT COMMENT '考试安排ID',
  room_id BIGINT COMMENT '考场ID',
  teacher_id BIGINT COMMENT '教师ID',
  role_type TINYINT COMMENT '角色类型(1-主监考,2-副监考)',
  check_in_status TINYINT DEFAULT 0 COMMENT '签到状态(0-未签到,1-已签到)',
  check_in_time DATETIME COMMENT '签到时间',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 请假记录表(leave_record)

```sql
CREATE TABLE leave_record (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  teacher_id BIGINT COMMENT '教师ID',
  leave_date DATE COMMENT '请假日期',
  time_slot VARCHAR(50) COMMENT '时间段',
  reason VARCHAR(500) COMMENT '请假原因',
  status TINYINT COMMENT '状态(0-待审核,1-已批准,2-已拒绝)',
  approve_by BIGINT COMMENT '审批人ID',
  approve_time DATETIME COMMENT '审批时间',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 消息通知表(notification)

```sql
CREATE TABLE notification (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT COMMENT '接收用户ID',
  title VARCHAR(100) COMMENT '通知标题',
  content TEXT COMMENT '通知内容',
  type TINYINT COMMENT '通知类型(1-系统通知,2-监考安排,3-变更提醒)',
  is_read TINYINT DEFAULT 0 COMMENT '是否已读(0-未读,1-已读)',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## 主要API设计

1. 用户认证
   - POST /api/auth/login - 用户登录
   - POST /api/auth/logout - 用户登出
   - GET /api/auth/info - 获取当前用户信息

2. 用户管理
   - GET /api/users - 获取用户列表
   - POST /api/users - 创建用户
   - PUT /api/users/{id} - 更新用户信息
   - DELETE /api/users/{id} - 删除用户

3. 教师管理
   - GET /api/teachers - 获取教师列表
   - POST /api/teachers - 添加教师信息
   - PUT /api/teachers/{id} - 更新教师信息
   - GET /api/teachers/{id}/invigilations - 获取教师监考记录

4. 考场管理
   - GET /api/rooms - 获取考场列表
   - POST /api/rooms - 添加考场
   - PUT /api/rooms/{id} - 更新考场信息
   - GET /api/rooms/{id}/schedule - 获取考场安排

5. 监考安排
   - GET /api/invigilations - 获取监考安排列表
   - POST /api/invigilations/auto-arrange - 自动排班
   - PUT /api/invigilations/{id} - 手动调整监考安排
   - DELETE /api/invigilations/{id} - 取消监考安排

6. 数据导入导出
   - POST /api/import/teachers - 导入教师信息
   - POST /api/import/rooms - 导入考场信息
   - GET /api/export/invigilations - 导出监考安排
   - GET /api/export/notifications - 导出监考通知

7. 请假管理
   - POST /api/leaves - 提交请假申请
   - PUT /api/leaves/{id}/approve - 审批请假申请
   - GET /api/leaves - 获取请假记录列表

8. 消息通知
   - GET /api/notifications - 获取通知列表
   - PUT /api/notifications/{id}/read - 标记通知为已读
   - POST /api/notifications/broadcast - 发送广播通知